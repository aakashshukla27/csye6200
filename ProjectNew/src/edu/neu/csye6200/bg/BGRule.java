package edu.neu.csye6200.bg;

public class BGRule {
    public static void calculateTrunkLength(Trunk trunk){
        //trunk.currentLength = (int) Math.sqrt(Math.pow((trunk.xf - trunk.xi), 2) + Math.pow((trunk.yf - trunk.yi), 2));
    }
}
